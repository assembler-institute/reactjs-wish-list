import styled from "styled-components"

export const Button = styled.button`
  width: auto;
  height: auto;
  background-color: transparent;
  border: none;

  &:hover {
    font-weight: 600;
    color: #a76bef;
  }
`;
